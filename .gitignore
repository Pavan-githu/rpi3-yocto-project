# Ignore all source layers (each has its own git repo)
sources/

# Ignore build artifacts, track only conf
build/tmp/
build/sstate-cache/
build/downloads/
build/cache/
build/bitbake-cookerdaemon.log

# build/conf/ is tracked
